<!--pages/index/wgwyld.wxml-->
<view class="container">
  <view class="big-image-container">
    <image src="{{img}}" 
           mode="aspectFit" 
           bindtap="previewImage"
           class="big-image"/>
    <view class="big-title">{{title}}</view>
  </view>
</view>